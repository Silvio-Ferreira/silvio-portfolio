{
    "builds": [
      {
        "src": "angular-project-root/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "angular-project-root/dist"
        }
      }
    ],
    "scripts": {
      "build": "npm install -g @angular/cli && ng build --prod"
    }
  }
  